diff --git a/server/mcp_tools.py b/server/mcp_tools.py
index 42d057b..be905c7 100644
--- a/server/mcp_tools.py
+++ b/server/mcp_tools.py
@@ -16,22 +16,23 @@ from core.config import settings

 # Create the FastMCP instance
 # We disable transport security (host validation) by explicitly passing a dictionary
-# configuring it to not require TLS or specific hosts, because we are running inside
-# a Docker network. We also set host="0.0.0.0" so it binds properly.
-import os
+# configuring it for the local systemd service.

 mcp = FastMCP(
     "paperless-mcp-server",
     stateless_http=True,
-    host="0.0.0.0",
-    port=8001,
+    host=settings.mcp_host,
+    port=settings.mcp_port,
     transport_security={
         "require_tls": False,
         "allowed_hosts": [
             "mcp-server",
             "mcp-server:8001",
+            f"{settings.mcp_host}:{settings.mcp_port}",
             "localhost",
-            "127.0.0.1"
+            "localhost:8001",
+            "127.0.0.1",
+            "127.0.0.1:8001"
         ]
     }
 )
