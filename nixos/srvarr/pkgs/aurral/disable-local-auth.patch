diff --git a/backend/middleware/auth.js b/backend/middleware/auth.js
--- a/backend/middleware/auth.js
+++ b/backend/middleware/auth.js
@@ -83,6 +83,11 @@ export const isProxyAuthEnabled = () => {
   return !!process.env.AUTH_PROXY_HEADER;
 };

+export const isLocalAuthEnabled = () => {
+  const value = String(process.env.DISABLE_LOCAL_AUTH || "").trim().toLowerCase();
+  return value !== "true" && value !== "1" && value !== "yes";
+};
+
 function parseCsv(value) {
   if (!value) return [];
   return String(value)
@@ -503,6 +508,7 @@ function migrateLegacyAdmin() {
 }

 function resolveUser(username, password) {
+  if (!isLocalAuthEnabled()) return null;
   if (userOps.countUsers() === 0) {
     migrateLegacyAdmin();
     if (userOps.countUsers() === 0) return null;
@@ -526,6 +532,7 @@ function resolveUser(username, password) {
 }

 function legacyAuth(username, password) {
+  if (!isLocalAuthEnabled()) return null;
   const authUser = getAuthUser();
   const passwords = getAuthPassword();
   if (passwords.length === 0) return null;
diff --git a/backend/routes/auth.js b/backend/routes/auth.js
--- a/backend/routes/auth.js
+++ b/backend/routes/auth.js
@@ -2,7 +2,7 @@ import express from "express";
 import { userOps } from "../db/helpers/index.js";
 import { createSession, deleteSession, getSessionByToken } from "../config/session-helpers.js";
 import { requireAuth } from "../middleware/requirePermission.js";
-import { getApiKey, rotateApiKey } from "../middleware/auth.js";
+import { getApiKey, isLocalAuthEnabled, rotateApiKey } from "../middleware/auth.js";
 import { hashPassword, verifyPassword, needsRehash } from "../middleware/passwordHash.js";

 const router = express.Router();
@@ -15,6 +15,9 @@ const getBearerToken = (req) => {

 router.post("/login", async (req, res) => {
   try {
+    if (!isLocalAuthEnabled()) {
+      return res.status(403).json({ error: "Local password login is disabled" });
+    }
     const username = String(req.body?.username || "")
       .trim()
       .toLowerCase();
     const password = String(req.body?.password || "");
     if (!username || !password) {
