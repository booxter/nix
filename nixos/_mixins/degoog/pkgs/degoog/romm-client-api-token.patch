diff --git a/plugins/romm/index.js b/plugins/romm/index.js
index bf67355d..5a3d7887 100644
--- a/plugins/romm/index.js
+++ b/plugins/romm/index.js
@@ -4,8 +4,6 @@ const thumb = createThumbCache();
 
 let rommUrl = "";
 let apiToken = "";
-let username = "";
-let password = "";
 let template = "";
 let resultItemTpl = "";
 
@@ -38,19 +36,9 @@ function searchVariants(term) {
   return [...new Set(variants)];
 }
 
-function _basicAuthValue(user, pass) {
-  const raw = `${user}:${pass}`;
-  const encoded =
-    typeof Buffer !== "undefined"
-      ? Buffer.from(raw, "utf8").toString("base64")
-      : btoa(raw);
-  return `Basic ${encoded}`;
-}
-
 function _authHeaders() {
-  if (apiToken) return { Authorization: `Bearer ${apiToken}` };
-  if (!username || !password) return {};
-  return { Authorization: _basicAuthValue(username, password) };
+  if (!apiToken) return {};
+  return { Authorization: `Bearer ${apiToken}` };
 }
 
 function _normalizeApiToken(token) {
@@ -165,29 +153,12 @@ export default {
     },
     {
       key: "apiToken",
-      label: "API Token",
+      label: "Client API Token",
       type: "password",
       secret: true,
-      required: false,
+      required: true,
       placeholder: "rmm_...",
-      description: "RomM client API token. Preferred over username/password.",
-    },
-    {
-      key: "username",
-      label: "Username",
-      type: "text",
-      required: false,
-      placeholder: "RomM login username",
-      description: "Legacy fallback if no API token is configured.",
-    },
-    {
-      key: "password",
-      label: "Password",
-      type: "password",
-      secret: true,
-      required: false,
-      placeholder: "RomM login password",
-      description: "Legacy fallback if no API token is configured.",
+      description: "Client API token with roms.read and me.read scopes",
     },
   ],
 
@@ -202,20 +173,18 @@ export default {
   configure(settings) {
     rommUrl = (settings.url || "").replace(/\/+$/, "");
     apiToken = _normalizeApiToken(settings.apiToken);
-    username = settings.username || "";
-    password = settings.password || "";
   },
 
   async isConfigured() {
-    return !!rommUrl && (!!apiToken || (!!username && !!password));
+    return !!rommUrl && !!apiToken;
   },
 
   async execute(args, context) {
     const fetchFn = context?.fetch || fetch;
-    if (!rommUrl || (!apiToken && (!username || !password))) {
+    if (!rommUrl || !apiToken) {
       return {
         title: "RomM Search",
-        html: `<div class="command-result"><p>RomM is not configured. Go to <a href="/settings">Settings → Plugins</a> and set your RomM URL plus an API token, or username and password for legacy login.</p></div>`,
+        html: `<div class="command-result"><p>RomM is not configured. Go to <a href="/settings">Settings → Plugins</a> and set your RomM URL and client API token.</p></div>`,
       };
     }
 
